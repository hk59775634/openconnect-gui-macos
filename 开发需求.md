# openconnect-gui-v2 开发需求

## 1. 项目定位

基于现有 VPN 客户端能力，开发一个**精简分支客户端**（openconnect-gui-v2）。

| 项 | 说明 |
|---|---|
| 协议范围 | **仅支持 SSLVPN**（OpenConnect） |
| 不包含 | WireGuard、其它协议、管理端 API 登录 |
| 目标 | 轻量、易用：账号密码本地保存 → 选节点 → 连接 |

---

## 2. 核心变更：去掉 API 登录

### 2.1 现状（对照）

现有客户端通过 API 登录获取登录信息与节点列表。

### 2.2 新方案

1. 用户提供/配置服务器根地址，记为 `$url`
2. 客户端下载 `$url/profile.xml`
3. 解析 `profile.xml`，得到：
   - 连接用 **URL**（VPN 网关地址）
   - **节点列表**
4. 账号、密码由用户本地输入并保存，**不再通过 API 获取**

### 2.3 登录三要素

| 要素 | 来源 | 说明 |
|---|---|---|
| **url** | `profile.xml` 解析 | VPN 连接地址 |
| **账号** | 用户输入 | 本地持久化保存 |
| **密码** | 用户输入 | 本地持久化保存 |

连接 SSLVPN 时，使用上述三要素发起连接。

---

## 3. 功能需求

### 3.1 账号密码

- 提供账号、密码输入界面
- 账号、密码**本地记住**（持久化），供后续连接使用
- 不通过 API 拉取或校验登录凭证（凭证仅在连接时交给 OpenConnect / SSLVPN）

### 3.2 profile.xml 与节点列表

- 节点数据唯一来源：`$url/profile.xml`
- 未登录：每次进入登录流程时拉取并解析 `profile.xml`
- 已登录：每次**重新启动 UI** 时自动再拉取一次 `profile.xml`，刷新节点列表
- 解析失败或下载失败时，需有明确错误提示，并允许重试

### 3.3 启动与页面流转

```
启动应用
    │
    ├─ 本地已有账号 + 密码 ──► 直接进入第二页（节点选择 / 连接）
    │                              │
    │                              └─ 启动时自动拉取 profile.xml，更新节点列表
    │
    └─ 本地无账号或密码 ──► 第一页（登录 / 配置页）
                               │
                               ├─ 输入账号、密码（及所需的 $url 配置）
                               ├─ 拉取 profile.xml 并解析节点
                               └─ 成功后进入第二页
```

| 状态 | 行为 |
|---|---|
| **未登录**（本地无完整账号密码） | 展示登录页；每次登录时拉取 `profile.xml` 解析节点 |
| **已登录**（本地有账号密码） | 跳过登录页，进入节点选择页；每次启动 UI 自动拉取一次 `profile.xml` 更新节点 |

### 3.4 连接

- 第二页：展示节点列表，用户选择节点后连接
- 连接参数使用：`profile.xml` 解析出的 url + 本地账号 + 本地密码
- 仅实现 SSLVPN 连接 / 断开；不实现其它协议

---

## 4. 非功能需求（建议）

| 项 | 说明 |
|---|---|
| 本地凭证安全 | 账号密码本地保存；建议加密存储（如系统凭据库 / DPAPI），避免明文 |
| 网络异常 | `profile.xml` 下载超时、HTTP 错误、XML 非法时给出可读错误 |
| 精简范围 | 不做 API 登录、不做多协议、不做与本需求无关的管理端功能 |

---

## 5. 明确不做

- API 登录 / Token / 管理端鉴权流程
- WireGuard 及其它非 SSLVPN 协议
- 通过 API 获取账号、密码或节点列表
- 依赖服务端「登录态」维持会话（本地仅存账号密码 + profile 节点）

---

## 6. 验收标准

1. 仅能使用 SSLVPN 连接，无其它协议入口。
2. 无 API 登录相关 UI 与逻辑。
3. 可通过下载并解析 `$url/profile.xml` 得到节点列表与连接 url。
4. 用户可输入账号、密码，并在本地记住；再次启动可自动进入节点页。
5. 未登录：每次登录都会重新拉取 `profile.xml`。
6. 已登录：每次重启 UI 都会自动重新拉取一次 `profile.xml` 并更新节点列表。
7. 选中节点后，使用「url + 账号 + 密码」三要素成功发起 SSLVPN 连接。

---

## 7. 待确认项（实现前可对齐）

> 以下不影响需求方向，实现时可再定。

1. `$url` 本身如何配置：首次手动输入？写死默认？还是从安装配置读取？
2. `profile.xml` 的字段结构与节点字段映射（需对照真实样例 XML）。
3. 「退出登录」是否需要：清除本地账号密码并回到登录页。
4. 本地账号密码的加密与存储方案（Windows 凭据管理器 / DPAPI / 其它）。
5. UI 技术栈与是否复用现有 FLink / openconnect-gui 工程。
